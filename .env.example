# Shared raw-layer settings, used by shared/scripts/extract_source.py.
# Copy to .env if you want to override:  cp .env.example .env
#
# Per-stack settings (ports, engine credentials) live in each stack's own .env.

AW_BAK_URL=https://github.com/Microsoft/sql-server-samples/releases/download/adventureworks/AdventureWorksDW2022.bak
AW_DATA_DIR=./shared/data/adventure_works_dw
MSSQL_SA_PASSWORD=Aw_DW_local_123!
